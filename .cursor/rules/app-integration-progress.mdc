---
alwaysApply: true
---

# CholoBD Expo — App Integration Progress

> Last audited: 2026-08-31  
> Method: Frontend git + API connectors (2026-07-31 → 2026-08-30) → mobile hooks/API usage + dashboard UI parity.  
> Hotel admin/employee parity re-check: 2026-08-31 (screens vs `HotelServiceAdminModule` / `HotelServiceEmployeeModule`).  
> Prior audit: 2026-08-30 / 2026-07-24.

Make sure to scan target file:  
1. NextJS-choloBD-frontend\.cursor\rules\app-module-integration-progress.mdc 
before making changes in this file.
Keep a record of this file and for every new writes in this file, check against target file. If differences are found, update target file copy info and work with the difference to product this file's report.
---

## 0. Frontend movement since last audit (web, last ~30 days)

These are **web** changes. Mobile should copy **API contracts and filters**, not the DOM/RQ UI.

### 2026-08-11 — homepage / booking chrome
- Homepage sections restyled; marketing copy moved to `/about-cholobd`
- Hero booking widget and booking pages refined
- Hotel booking URL bug fixed
- Web mobile-width menus / navbar / notifications polished
- Deploy-blocking bugfix

### 2026-08-27 — tour builder + paginated lists + transport
- **Tour builder split:** catalog (`/tour-builder` + `/tour-builder/platform`) vs personal (`/tour-builder/my` + `/tour-builder/custom`)
- Catalog/personal CRUD, image delete, richer day-segment payload (`shortDescription`, `segmentOrder`, `hotelId`, `transportId`, `notes`)
- Shared `unwrapPaginatedList`: list `data` is now `{ results, total, page, limit }` (not a bare array)
- Applied on: hotels, tour spots, activity spots, guides, tour builder, transports
- Admin/asset list pages: pagination + division / name / budget filters
- **New web APIs (not on mobile yet):** `transportApi`, `transportInventoryApi`, `transportBookingApi`
- Bus service-admin UI (management / rides / tickets)
- Transport CRUD pages (`/transports`)

### 2026-08-27 + follow-up — homepage tours
- `GuideSuggestions` typed against unwrapped `Guide[]` (not `.results` on an array)
- `TourSuggestions` fetches via `useGetAllTourPlansRQ` (client), not Server Component `getAllTourPlans`
- `getAllTourPlans` uses `cache: "no-store"` so production `/` cannot bake a stale list shape

### Frontend API barrel (`services/api/index.ts`) now exports
Previously missing from the barrel, now present: `GuideBookingApi`, `TransportApi`, `TransportInventoryApi`, `TransportBookingApi`.  
Still not in the web barrel: trip planner, package bookings (mobile remains ahead there).

---

## 1. Verdict

Mobile still covers the **core traveler loop**: auth, locations, hotels + hotel booking, tour spots, **catalog** tour builder + package booking, trip planner, community, payments (SSL init), hotel service-admin (profile, current bookings, QR), and **guides / guide booking** (browse, request, pay, operator actions).

Largest remaining gaps vs current frontend: **transport** stack still mostly shells; **reviews CRUD**, **bookmarks**, **notifications**, **wallet**, **addresses**, **search**, **categories/config**; **master-admin modules**. Hotel / guide / activity service-admin core tabs are integrated (guide+activity hub gated by `serviceType`).

---

## 2. Backend modules vs mobile integration

| Backend route prefix | Frontend API | Mobile API / hooks | Status |
|---|---|---|---|
| `/auth` | AuthApi | oauth + authSlice | Done |
| `/users` | UserApi | `users.ts`, service-admin hooks | Partial (profile / my-hotel; no full user mgmt) |
| `/config` | ConfigApi | — | Missing |
| `/locations` | LocationApi | `locations.ts`, `useFetchLocations` | Done (unwraps paginated `data` if present) |
| `/categories` | CategoryApi | — | Missing |
| `/tour-spots` | TourSpotApi | `tourSpots.ts`, `useFetchTourSpots` | Done (read + detail; reviews display-only; list unwrap + name/type/page filters) |
| `/activity-spots` | ActivitySpotApi | `activitySpots.ts`, `useFetchActivitySpots` | Partial (read for planner/builder; unwrap + filters; no standalone browse/book) |
| `/guides` | GuideApi | `guides.ts`, `useFetchGuides`, `useFetchGuideDetail`, `useGuideAdminLogic` | Done (browse, detail, availability, my-profile, availability update; `divisionId` filter) |
| `/hotels` | HotelApi | `hotels.ts`, `hotelDetail.ts`, fetch hooks | Done (list unwrap + division/name/page filters) |
| `/hotel-rooms` | HotelRoomApi | `hotelRooms.ts` + `useHotelRoomManagement`; `users.getHotelRooms` | Done for room-type CRUD + room status updates (admin + employee) |
| `/bookings/hotel-rooms` | HotelBookingApi | `bookings.ts`, `hotelBookings.ts`, booking hooks | Done |
| `/bookings/activity-spots` | ActivitySpotBookingApi | `activityBookings.ts`, `useFetchActivityBookings`, `useActivityBookingLogic`, booking/list/detail screens | Done |
| `/bookings/guides` | GuideBookingApi | `guideBookings.ts`, `useGuideBookingLogic` | Done (request, list, detail, cancel, status actions, SSLCommerz pay) |
| `/tour-builder` | TourBuilderApi | `tourBuilder.ts`, `useTourBuilderLogic`, `useFetchTourPackages` | Partial — catalog CRUD/list Done (paginated unwrap + division/tourSpot/page filters). Personal `/my` API + `useFetchPersonalTourPlans` / `usePersonalTourPlanLogic` added; **no custom-tour screens** |
| `/trip-plans` | *(not in frontend api index)* | `tripPlanner.ts`, `useTripPlannerLogic` | Done (mobile ahead of web API barrel) |
| `/bookings/package-bookings` | *(not in frontend api index)* | `packageBookings.ts`, `usePackageBookingLogic` | Done (mobile ahead of web API barrel) |
| `/transports` | TransportApi | — | Missing (web added 2026-08-27) |
| `/transport-inventory` | TransportInventoryApi | — | Missing (classes, routes, layouts, trips, vehicles) |
| `/bookings/transports` | TransportBookingApi | — | Missing |
| `/reviews` | ReviewApi | nested on tour-spot payloads only | Missing (no review CRUD API) |
| `/community` | CommunityApi | `community.ts` + community hooks | Done |
| `/bookmarks` | BookmarkApi | — | Missing |
| `/notifications` | NotificationApi | — | Missing |
| `/complaints` | ComplaintApi | `complaints.ts` + `useComplaintInboxLogic` + admin/employee UI | Done (hotel inbox/detail/status/comments) |
| `/search` | SearchApi | homepage search UI not wired | Missing |
| `/addresses` | AddressApi | — | Missing |
| `/payments` | PaymentApi | `payments.ts`, `usePaymentLogic` | Done |
| `/wallets` | WalletApi | side-menu “Coming Soon” | Missing |

---

## 3. Mobile hooks — actively used

All listed hooks under `src/hooks/` are imported by screens/components unless marked new/unused.

**Integrated domain hooks:** 
`useAuthInitializer`, `useGoogleSignIn`, `useFacebookSignIn`, `useAuthWithAdminCheck`, `useFetchLocations`, `useFetchHotels`, `useFetchHotelDetail`, `useFetchLocationHotels`, `useFetchUserHotelBookings`, `useBookingLogic`, `useFetchTourSpots`, `useFetchActivitySpots`, `useFetchTourPackages`, `useTourBuilderLogic`, `useDaySegmentSpots`, `usePackageBookingLogic`, `useTripPlannerLogic`, `useFetchCommunityPosts`, `useFetchCommunityPost`, `useCommunityPostLogic`, `useAdminCommunityLogic`, `useFetchGuides`, `useFetchGuideDetail`, `useGuideBookingLogic`, `useGuideAdminLogic`, `useDashboardLogic`, `useCurrentBookingsFetch`, `useServiceAdminLogic`, `usePaymentLogic`, `useQRGeneration`, `useQRScanner`, `useCameraPermission`, `useTheme`, `useLanguageSwitcher`, `usePreloadAssets`, `useFetchPersonalTourPlans`, `usePersonalTourPlanLogic`, `useComplaintInboxLogic`, `useHotelRoomManagement`, `useFetchActivityBookings`, `useActivityBookingLogic`, `useFetchTransports`, `useFetchTransportDetail`, `useTransportLogic`

**Transport dashboard screens added 2026-08-30:**
- Service admin: `transport-admin`, `transport-inventory`, `transport-rides`, `transport-tickets`, `transport-sales` (placeholder features)
- Employee: `employee/transport-operations` (tabs: seat plan, operations, checkin, maintenance - all placeholder)

---

## 4. Mobile API connectors present

`oauth`, `users`, `locations`, `hotels`, `hotelDetail`, `bookings`, `hotelBookings`, `activityBookings`, `tourSpots`, `activitySpots`, `tourBuilder` (catalog + **personal `/my`**), `packageBookings`, `tripPlanner`, `community`, `communityMedia`, `payments`, `guides`, `guideBookings`, `transports`, `complaints`, `hotelRooms`, `qr`, `translation`, uploads (`cloudinaryUpload`, `firebaseUpload`), `axiosClient`

Shared list unwrap: `src/utils/paginatedList.ts` (`unwrapListData`) — use on any endpoint that may return `T[]` or `{ results, total, page, limit }`.

**Not present (frontend already has):** category, review, search, wallet, address, config, bookmark, notification, **transportInventory**, **transportBooking**  
**Present since hotel admin sweep:** `hotelRooms.ts`, `complaints.ts`

---

## 5. Dashboard UI parity

### Frontend `modular-components/dashboard` → Mobile equivalent

#### User dashboard
| Frontend module | Mobile equivalent | Status |
|---|---|---|
| Hotel booking history / BookedHotels | `user-bookings`, tracking hotel-bookings, `BookingCard` | Done |
| Package bookings *(web API barrel gap)* | `package-bookings` screens + cards | Done |
| Personal / custom tour plans | Custom tour screens + hooks + forms | Done |
| StatsOverview | `UserStatsOverview` component with real booking aggregation | Done |
| Activity booking history / BookedActivity | `activity-bookings`, `activity-bookings/[bookingId]`, `ActivityBookingCard` | Done |
| Guide booking history / BookedGuide | `dashboard/guide-bookings` + `guide-bookings/[bookingId]`, `GuideBookingCard` | Done |
| Transport booking history / BookedTransport | TransportTab empty-state only | Missing (web now has TransportBookingApi) |
| BookmarksSection | — | Missing |
| SubmittedComplaintsSection | — | Missing |
| TransactionHistorySection | — | Missing |
| BookingPostStayActions | — | Missing |
| UserActivityHistoryModule | — | Missing |
| AddressManagerModule | — | Missing |

Mobile user hub: `UserDashboard` → cards for my bookings, package bookings, explore, trip planner.  
`bookings` prop is passed into `UserDashboard` but **not rendered** (see §6).

#### Service-admin (hotel) — ~95% (re-checked 2026-08-31)

Web `HotelServiceAdminModule` tabs: **profile | rooms | earnings | complaints** (+ top `AdminStatsDashboard`, which is still fake/`isReady={false}` on web).

| Frontend module | Mobile equivalent | Status |
|---|---|---|
| Hotel profile (`HotelProfileSection`) | `hotel-info` + **`hotel-edit`** (`updateHotelInfo` / `updateHotelCoreInfo`) | Done (view + edit contact/hours/description) |
| Room type management (`HotelRoomTypeManagementModule`) | `room-types` + `room-type-form` (+ Cloudinary images) | Done |
| Room type stats (`RoomTypeStatsSection`) / AdminStats | `hotel-stats` (real booking aggregation) | Done equivalent |
| Earnings (`EarningsSummarySection`) | `hotel-earnings` | Done |
| Customer complaints (`ComplaintManagerModule`) | `complaints` + detail + `complaints.ts` | Done |
| Current bookings *(ops)* | `current-bookings` → booking detail | Done |
| QR check-in | `qr-scanner` | Done |
| Staff / employees *(mobile-only)* | `staff.tsx` unavailable UX (no roster API) | Blocked on backend |
| Owner “your bookings” | `your-bookings.tsx` personal bookings | Done |

Hub wiring: `ServiceAdminDashboard` cards link to hotel-info hub, current-bookings, hotel-stats, hotel-earnings, room-types, **complaints**, QR, staff, your-bookings (plus guide/transport cards).

#### Guide service-admin — ~90% (re-checked 2026-08-31)

Web `GuideServiceAdminModule` tabs: **profile | bookings | earnings**.

| Frontend module | Mobile equivalent | Status |
|---|---|---|
| GuideProfileSection | `service-admin/guide-profile` (view + working-day editing) | Done |
| BookingRequestManagement | `service-admin/guide-requests` (accept / decline / complete) | Done |
| GuideStatsDashboard + EarningsSummarySection | `guide-requests` stats header + dedicated `guide-earnings` | Done |
| GuideForm (full profile CRUD) | — | Missing (web Edit → `/guides/:id/edit`; master-admin provisioning stays on web) |

Hub: `ServiceAdminDashboard` shows guide cards when `serviceType` is `GUIDE_SERVICE`.

#### Activity service-admin — ~85% (added 2026-08-31)

Web `ActivityServiceAdminModule` tabs: **profile | bookings | earnings**.

| Frontend module | Mobile equivalent | Status |
|---|---|---|
| ActivityProfileSection | `activity-profile` (view + inline edit via `updateActivitySpot`) | Done |
| ActivityStatsDashboard / BookingRequestManagement | `activity-bookings-admin` (pending/confirmed/history + cancel) | Done |
| EarningsSummarySection | `activity-earnings` | Done |

Hub: activity cards when `serviceType` is `ACTIVITY_BOOKING`. APIs: `getActivitySpotDetail` / `updateActivitySpot` + existing `activityBookings.ts`.

#### Bus / transport service-admin — partial (2026-08-30)
- **Added:** `transport-admin.tsx` main dashboard with stats
- **Added:** Placeholder screens: `transport-inventory`, `transport-rides`, `transport-tickets`, `transport-sales`
- **Status:** Infrastructure in place; full CRUD features pending
- Frontend `BusServiceAdminModule` has detailed bus classes, routes, layouts, trips management — not yet ported to mobile

#### Hotel employee — ~90% (re-checked 2026-08-31)

Web `HotelServiceEmployeeModule` tabs: **rooms | bookings | complaints | maintenance** (+ metrics dashboard).

| Frontend module | Mobile equivalent | Status |
|---|---|---|
| Metrics (`HotelMetricsDashboard`) | `HotelMetricsTab` | Done |
| Room status (`HotelRoomStatusManagement`) | `RoomStatusTab` + `useHotelRoomManagement` | Done |
| Bookings (`HotelRoomBookingsManagement`) | `BookingsTab` (expand detail, search, open detail) | Done |
| Maintenance (`HotelMaintenanceTasksManagement`) | `MaintenanceTab` | Done equivalent — local demo tasks + banner (web also fake; **no maintenance API**) |
| Complaints (`ComplaintManagerModule`) | `ComplaintsTab` → shared detail route | Done |

Hub: `EmployeeDashboard` → `employee/hotel-operations` (tabbed).

#### Transport employee — partial (2026-08-30)
- **Added:** `employee/transport-operations.tsx` with tab navigation (seat plan, operations, checkin, maintenance)
- **Status:** Shell only; all tabs show "under development" placeholder
- Frontend `BusServiceEmployeeModule` has detailed seat maps, ride operations, check-in, maintenance — not yet ported to mobile

#### Not started on mobile (frontend has UI)
- **Master-admin** modules (users, locations, categories, wallet, site config, complaints)

---

## 6. Incomplete / broken / improperly interfaced UI

### Broken or stub screens
- Side menu `wallet` / `payment` — Alert “Coming Soon” (`app/(tabs)/index.tsx`)
- Home `QuickActionGrid` transport action — disabled “Soon”
- `cloudinaryUpload` delete — explicitly not implemented
- Hotel `staff.tsx` — honest “unavailable” (no SERVICE_ADMIN staff-list API; Master Admin assigns employees)
- Hotel `MaintenanceTab` — local demo data only (parity with web fake tasks; no backend)

### Improper / dead interfacing
- `BookingHistoryUI` exists but is **never imported** (consider removing)
- Orphan homepage components (not in active home composition): `HeroSection`, `FeaturesGrid`, `FeatureCard`, `ImageCarousel`, `NearbyLocationsSection` (fake Unsplash data), `SearchSection`, `QuickBookingWidget`, `TransportTypeSelector`, `LocationCard`
- Orphan explore files: `UserExploreInterface.tsx`, `AdminExploreInterface.tsx` (debug `console.log`s); live path uses `ExploreInterface.tsx` only (`explore/index.ts` aliases to that)
- `FeaturesGrid` links to `/(shop)/wallet` — route does not exist
- Trip planner `TransportTab` — display-only; no transport booking API

### Partial UX (works but incomplete vs web)
- Reviews: display on tour spot detail only; no create/update
- Activity spots: selectable in planner/builder; no standalone browse/book flow
- Catalog tour list: API unwraps pagination; explore UI still has no division / page-size chrome like web
- Personal tour packages: screens + forms exist (see explore personal-tour-*)
- Service-admin hotel: profile + room-type CRUD + stats + earnings + QR + current bookings + **complaints** Done; staff = unavailable UX (no API); your-bookings wired to personal bookings
- Homepage search field present in leftover `SearchSection` but not on live home; no `/search` connector

---

## 7. Homepage Sections Gap Analysis (2026-08-30)

**Context:** Mobile app homepage currently shows fewer sections than frontend. Not all are necessary for mobile UX, but gaps should be reviewed when prioritizing work.

### Currently Present in Mobile
- ✅ HomeHeader, HeroBackground, QuickActionGrid
- ✅ ExploreBDBanner (Explore BD + Community)
- ✅ TourPackagesSection, SuggestedToursSection

### Missing from Mobile (Frontend has)
| Section | Priority | Notes |
|---------|----------|-------|
| HotelDealsSection | **HIGH** | Shows popular hotels with auto-scroll; hooks exist (`useFetchHotels`) |
| ActivitySuggestionsSection | **HIGH** | Activity spots showcase; hooks exist (`useFetchActivitySpots`) |
| GuideSuggestionsSection | MEDIUM | Promotes guide bookings; hooks exist (`useFetchGuides`) |
| TransportTicketsSection | LOW | Can wait until transport booking is functional |
| FAQSection | MEDIUM | Static accordion; helps onboarding |
| MobileAppIntroduction | N/A | Web-specific (promotes downloading app) |

### Orphaned/Unused Mobile Components
Already built but not integrated into homepage: `HeroSection`, `FeaturesGrid`, `SearchSection`, `QuickBookingWidget`, `NearbyLocationsSection`, `LocationCard`, `ImageCarousel`, `TransportTypeSelector` — audit/cleanup needed.

**Action Items (when prioritizing homepage work):**
1. Implement HotelDealsSection + ActivitySuggestionsSection (highest impact)
2. Add GuideSuggestionsSection (medium priority)
3. Consider FAQ section for user onboarding
4. Decide on orphaned components (delete or repurpose)

---

## 8. Suggested integration priority (remaining)

1. **Transport (large, do as its own sweep):** `transportInventory.ts` / `transportBookings.ts` + browse/book + bus service-admin detailed UI 
2. **Homepage sections:** HotelDealsSection, ActivitySuggestionsSection (see §7)
4. **Engagement:** reviews CRUD, bookmarks, notifications  
5. **Account extras:** wallet, addresses, user-submitted complaints UI, real search  
6. **Dashboard polish:** wire `BookingHistoryUI` or drop dead props  
7. **Other roles (if in scope):** master-admin modules 
8. **Cleanup:** remove or reconnect orphan homepage/explore components  
9. **Backend-gated:** hotel staff roster API (if SERVICE_ADMIN should list employees); real maintenance API 
---

## 8. Rough coverage estimate

| Area | Approx. |
|---|---|
| Backend modules with meaningful mobile usage | ~14 / 29 route groups |
| Frontend API connectors mirrored on mobile | ~14 / 26 exported APIs (+ trip/package ahead of web barrel) |
| User traveler features (core) | ~78% |
| User dashboard parity vs web modular dashboard | ~50% |
| Hotel service-admin parity | ~95% (staff list blocked on backend; else Done) |
| Hotel employee parity | ~90% (maintenance demo-only like web; complaints Done) |
| Guide service-admin parity | ~90% |
| Activity service-admin parity | ~85% |
| Catalog tour builder | ~75% (list contract aligned; form still older than web 2026-08-27) |
| Personal tour builder | ~60% (screens/forms present; polish TBD) |
| Transport / bus | ~15% (shells + types/hooks; inventory/booking UI thin) |
| Other service-admin / employee / master-admin | master-admin ~0% |
